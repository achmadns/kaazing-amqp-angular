{
    "name": "angular-tryout",
    "version": "0.1.0",
    "scripts": {
        "prestart": "npm install",
        "postinstall": "bower install",
        "start": "cd app; http-server -p 8001",
        "pretest": "npm install",
        "test": "karma start conf.karma.js --single-run",
        "preupdate-webdriver": "npm install",
        "update-webdriver": "webdriver-manager update",
        "preprotractor": "npm run update-webdriver",
        "protractor": "protractor conf.protractor.js"
    },
    "engines": {
        "node": ">=0.10.20"
    },
    "dependencies": {

    },
    "devDependencies": {
        "karma": "~0.10",
        "protractor": "^1.1.1",
        "http-server": "^0.6.1",
        "bower": "^1.3.1",
        "shelljs": "^0.2.6",
        "karma-junit-reporter": "^0.2.2"
    }
}
